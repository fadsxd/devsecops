# Proyecto DevOps Local

## Requisitos
- Docker
- Docker Compose

## Ejecutar
```bash
docker-compose up --build

App: http://localhost:8080
Grafana: http://localhost:3001
Prometheus: http://localhost:9090
Kibana: http://localhost:5601



Antes de correr:

```bash
sudo chmod -R 777 .

docker-compose up --build